services:
  qdrant:
    image: qdrant/qdrant:latest
    restart: unless-stopped
    ports:
      - "6333:6333" # REST
      - "6334:6334" # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    healthcheck:
      test: ["CMD", "bash", "-lc", "echo > /dev/tcp/localhost/6333"]
      interval: 10s
      timeout: 3s
      retries: 20
    networks:
      - infra

  glean-server:
    image: ${GLEAN_IMAGE:-glean-base}
    build:
      # Point this at a local checkout of https://github.com/withakay/glean-docker.
      # Recommended: create a symlink at ./.local/glean-docker (repo root).
      context: ${GLEAN_DOCKER_PATH:-./.local/glean-docker}/base
      dockerfile: Dockerfile
    restart: unless-stopped
    entrypoint: ["glean-server"]
    command:
      - --db-root
      - /data/glean/db
      - --port
      - "12345"
    ports:
      - "12345:12345"
    volumes:
      - glean_db:/data/glean/db
    healthcheck:
      test: ["CMD", "bash", "-lc", "echo > /dev/tcp/localhost/12345"]
      interval: 10s
      timeout: 3s
      retries: 30
    networks:
      - infra

  glass-server:
    image: python:3.12-slim
    restart: unless-stopped
    depends_on:
      glean-server:
        condition: service_healthy
    command: ["python", "/app/server.py", "--port", "12346"]
    ports:
      - "12346:12346"
    volumes:
      - ./docker/glass-stub:/app:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.create_connection(('127.0.0.1',12346),1); s.close()"]
      interval: 10s
      timeout: 3s
      retries: 30
    networks:
      - infra

  ollama:
    profiles: ["ollama"]
    image: ollama/ollama:latest
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "ps"]
      interval: 10s
      timeout: 3s
      retries: 30
    networks:
      - infra

volumes:
  qdrant_storage:
  glean_db:
  ollama_data:

networks:
  infra:
    name: code-context-infra
